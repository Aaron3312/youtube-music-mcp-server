runtime: typescript

# User configuration for YouTube Music integration
config:
  debug:
    type: boolean
    default: false
    description: "Enable debug logging for troubleshooting"

  cookies:
    type: string
    required: false
    description: "YouTube Music authentication cookies (REQUIRED for functionality - get from music.youtube.com after logging in)"
    sensitive: true